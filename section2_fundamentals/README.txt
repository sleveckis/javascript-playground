To "load" any of the .js files for display with Live Server or a browser,
just change the line in 'index.html' to reflect the desired .js file.

To see where we're printing stuff with console, press F12 with the broswer open
and go to the console entry there.

Neat!